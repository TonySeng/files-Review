# CHANGELOG final28 — 跨文件审查缺件前置校验

## 背景
用户指出：审核规则涉及跨文件内容审查时（如 dec-09 三方文档名称一致、dec-10 中标金额一致、
dec-05/06/07/12 等一致性规则），应先对送审文件清单名称做一轮预处理筛选，确认与审核相关的
文件均已包含在送审范围内，避免因缺少指定文件而导致误审或产生不完整的审核结果。

## 改动

### 1. 提示词（`prompt_store._BUILTIN_REVIEW_RULE`，v8→v9）
- 新增「送审文件清单」区块：渲染时注入本次实际送审的文件名及推断文档角色，供模型核对。
- 新增【跨文件审查前置校验(缺件预筛·防误审/不完整结果)】纪律块：
  - 规则说明/审核要点出现多份文档名称即属跨文件审查，结论前必须先核对清单确认必需文件齐全；
  - 必需文件缺失时严禁凭空比对或单边不完整比对，必须输出 status=unknown 并说明缺哪个文件角色；
  - 「（如有）」声明的可选文件缺失时仍可比对已收到文件，但须注明缺失、不得断言"完全一致"；
  - 严禁把清单之外的文件名写入 evidence/location/involved_files。

### 2. 后端预筛（`review_engine.py`）
- 新增文档角色关键词表 `_DOC_ROLE_KEYWORDS`（评审报告/评标报告/定标结果/定标审批/定标批复/
  中标通知书/招标文件/投标文件/资格证明/承诺书）与可选标记 `_OPTIONAL_ROLE_MARKERS`。
- `_infer_doc_roles(doc)`：从文件名/类型推断文档角色。
- `_rule_required_roles(rule)`：从规则名称/说明抽取必需与可选文档角色（可选以「（如有）」等
  紧邻标记判定，窗口收紧到关键词后 8 字，避免同句其他角色的标记被误判）。
- `_is_cross_file_rule(rule)`：一致性类或说明含「跨文档/跨文件」即视为跨文件规则。
- `_cross_file_precheck(rule, docs)`：对送审清单做缺件预筛，返回必需/可选角色、命中角色、
  缺失角色与「有把握判定缺失(confident_missing)」标志。
- `_make_cross_file_missing_finding(rule, pc, docs)`：构造受控 unknown 结论（status=unknown，
  title 注明缺哪个文件角色，detail 列出本次已收/缺失文件，suggestion 提示补件重审）。
- `_build_file_manifest(docs)`：构造送审文件清单文本（含推断角色）。
- 拦截策略（避免文件名泛化时误拦截）：
  - 规则未声明任何必需角色（如 dec-11 仅写「各文档」）→ 不拦截；
  - 单文档内部一致性规则（必需角色仅 1 个）→ 仅当该角色缺失且能识别到其他角色文件时拦截；
  - 多文档跨文件规则（必需角色≥2）→ 仅当可比对文档数 < 2（不足以做跨文件比对）时拦截，
    仅缺可选/额外来源文件（如 dec-09 缺中标通知书仍可比对报告vs定标、dec-10 缺中标通知书
    仍可比对报告vs定标）不拦截，交由模型按纪律标注。

### 3. 接入点
- `process_batch`：调用 LLM 前对每条跨文件规则做 `_cross_file_precheck`；`confident_missing`
  为真时直接给出受控 unknown 并排除出 LLM 批次（不送模型，杜绝臆造/单边不完整比对）。
- 一致性核查引擎（`run_consistency`）：入口对一致性规则做同样预筛，必需文件缺失时跳过该规则
  比对并补一条受控 unknown，避免单边不完整比对。
- `build_rule_prompt` 新增 `file_manifest` 参数，三个 prompt 构建入口
  （`_run_rule_prompt_guarded` / `_run_rule_per_file` / `_proofread_by_segments`）透传，
  渲染注入「送审文件清单」区块。

## 验证
- `py_compile` 三文件通过。
- 单元自测（覆盖 dec-04/05/06/07/09/10/11/12/17）：
  - dec-09 全齐→放行；仅评审报告(缺定标结果)→拦截；评审报告+定标结果(缺可选中标通知书)→放行。
  - dec-10 评审报告+定标结果(缺中标通知书)→放行；仅评审报告→拦截。
  - dec-06 仅定标结果(缺招标文件)→拦截；定标+招标→放行。
  - dec-04 仅评审报告(单文档规则)→放行；泛化命名→不硬拦截(交模型)。
  - dec-11 无明确角色声明→不拦截。
- 内置模板含新纪律块与清单区块；`render` 注入 `file_manifest` 后占位符被替换。

## 生效方式
1. 后端重建镜像并重启（已编入源码，docker-compose up -d --build 即可）。
2. `POST /api/prompts/review_rule/reset` 重置为内置 v9（含新纪律块与清单区块）。
3. 历史任务快照不回改，重跑即按新逻辑。
