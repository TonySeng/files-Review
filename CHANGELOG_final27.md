# 变更记录 final27 —— 修复结论与规则说明脱节/模型被招标文件摘要带偏

> 目标：解决审核结果中规则名（如 `dec-03 评审打分异常与一致性`）与结论内容（如「人员要求不符合招标文件要求」「否决投标情形」）完全脱节的问题，确保每条 finding 的 title/detail/evidence 严格对应当前 rule_id 的规则说明与审核要点。

## 一、问题根因

- `dec-03 评审打分异常与一致性` 的 `rule_name` 来自后端按 `rule_id` 查规则集；`title/detail` 由模型生成。
- 投标审核模式下，每个规则的 prompt 都会注入「招标文件关键要求」摘要（含人员要求、资格要求、业绩要求、否决投标情形等标签）。
- 模型在多规则审核时被这些摘要标签带偏，把「人员要求」「否决投标情形」等本不属于当前规则的内容写到了 `dec-03` 等规则的 finding 里，造成规则说明/审核点与实际结论脱节、原文定位也失去意义。

## 二、改动内容

### 2.1 提示词纪律层（`backend/services/prompt_store.py`，`_BUILTIN_REVIEW_RULE` v7 → v8）

新增 **【规则对应纪律（防止结论挂错规则/被招标文件摘要带偏）】**：

1. 每条 finding 的 `title/detail/evidence` 必须严格对应当前 `rule_id` 的规则说明与审核要点；不得把「招标文件关键要求」摘要中的人员要求、资格要求、业绩要求、否决投标情形、实质性条款等标签直接作为本规则的结论输出。
2. 以 `dec-03` 为例：结论只能围绕「打分/评分/分值/评委/离群/趋同」等本规则对象；发现人员资格、否决投标、业绩等问题时应判 `pass`，不得写入本规则结论。
3. 输出前自检：若 conclusion 内容与当前 `rule_id` 的规则说明/审核要点无法对应，必须将该条结论改为 `status=pass`、`title="本规则未触发"`、清空 `detail/evidence/location`。

> 原第 8 条「要素来源与抽取纪律」顺延为第 10 条，编号相应调整。

### 2.2 tender_block 使用方式优化（`backend/services/prompts.py`）

`build_rule_prompt` 渲染的「招标文件关键要求」摘要前增加显式标注：

> 「仅作背景理解，当前规则的具体审核对象与判定标准以上文「审核规则」为准，不得把其中的人员要求、资格要求、业绩要求、否决投标情形等摘要标签直接作为本规则的结论」

明确告诉模型：摘要只是背景，不是当前规则的审核对象。

### 2.3 后端兜底校验（`backend/services/review_engine.py`）

新增 `_tender_label_mismatch(item, rule)` 检测：

- 维护 `_TENDER_LABELS` 常量（人员要求、资格要求、业绩要求、否决投标情形、实质性、投标保证金、投标有效期、工期要求、签章要求、投标文件组成、关键时间节点、最高限价、预算、项目名称、项目编号等）。
- 若 finding 的 `title/detail` 中出现了上述标签，但当前规则的 `name/description/checkpoints` 中完全没有该标签，则判定为「被招标文件摘要带偏/结论挂错规则」。
- 对 `status=fail/warn` 的此类结论，自动降级为 `unknown`，并在 `detail` 中追加系统标注，提示人工复核。
- 正常规则（如 `dec-02` 自身描述含「否决投标情形」）不会被误伤。

## 三、生效方式

1. **提示词**：`POST /api/prompts/review_rule/reset`（重置为内置默认 v8）方可生效。
2. **后端代码**：需重新构建 Docker 镜像（`docker-compose up -d --build`）或热替换源码。
3. 规则集文案无需再次迁移（未修改 `gen_decision_rules.py`）。
4. 历史任务快照不回改，重跑即按新逻辑。

## 四、回归关注点

- `dec-03 评审打分异常与一致性` 的 finding 是否只讨论打分/评分/分值/评委/离群/趋同，不再出现「人员要求」「否决投标情形」等无关结论。
- 各规则的 `title/detail/evidence` 是否与该规则说明/审核要点一一对应。
- `dec-02`「否决投标情形」、自定义人员/资格类规则等正常结论不被误降级。
