# 审核系统三项优化 · 回归说明文档

> 生成日期：2026-09-02 ｜ 范围：进度显示异常 / 性能瓶颈 / 长文档并行结果合并 + 两个衍生修复
> 状态：全部落地、重建、真实验证通过（双服务 `:9090` / `:9100` 健康 200）

---

## 0. 背景与目标

用户提出三项审核系统优化诉求：

1. 排查审核任务执行后进度一直显示 0%、未正常更新的原因并修复；
2. 分析单文件含 20 规则却执行 >15 分钟的瓶颈并优化；
3. 优化长文档拆分并行审核时同规则 N 条结果的输出逻辑，确保 20 规则最终仅输出 20 条。

收尾阶段新发现并修复两个衍生根因 bug（**登录后任务列表永远为空**、**PDF 导出整条 500**），使三项优化在「前端可见 + 导出交付」层面真正闭环。

---

## 1. 优化①：进度一直显示 0% / 不更新

### 根因
- 进度仅在「每条规则批次开始」按 `(idx-1)/total` 更新，长耗时批次（分段校对数分钟）处理中无任何进度事件 → 观感卡 0%；
- 并发下基于起始序号的进度会回跳；
- `_build_on_event` 对缺 `progress` 字段的 stage 事件默认置 0，子步骤事件把进度重置为 0。

### 改动
- `backend/routers/review.py:_build_on_event`：`if "progress" in evt:` 才更新 progress；否则只更 `progress_message`（**不再重置为 0**）。
- `backend/services/review_engine.py`：并发批次改 `asyncio.as_completed`，按「已完成批次数/total」单调推进；`_proofread_by_segments` 逐段完成实时发进度（total 分母，单调不回跳）；一致性阶段进度沿用 total 分母衔接。

### 验证
- 25 规则任务：进度 `2% → 4% → … → 96% → 100%` 平滑推进，无卡 0%。
- 真实新任务 `8261c6fddbec`：`t+0s progress=80 running` → `t+30s progress=100 completed`；`monotonic / nonzero / reached100` 全 `True`。

---

## 2. 优化②：单文件 20 规则 >15 分钟瓶颈

### 根因
3 条校对类规则（`gen-typo` / `gen-semantics` / `gen-terminology`）各自成批，分别对整文档做分段并行校对 → `3 × N_seg` 次 LLM 调用，受 `llm_max_concurrent=10` 多轮排队；单段偶发 180s 读超时 + 重试放大耗时。

### 改动
`review_engine.py`：全部校对类规则合并为单个批次（`batches.append(editing_rules)`），一次分段并行覆盖所有校对规则，调用量从 `N_rules × N_seg` 降至 `N_seg`（≈3× 提速）；`_proofread_by_segments` 已按 `rule_id` 累加合并各段结论，质量不受影响。

### 验证
59,457 字文档 + 25 规则（含 3 校对）：整任务 **484s（≈8 分钟）** 完成，远低于 15 分钟。

> 取舍：合并后 `gen-typo` 召回略降 34→29（模型同段兼顾三类校对），属可接受的精度/速度权衡；若错别字召回优先，可改回分别成批。

---

## 3. 优化③：长文档拆分并行 → 同规则 N 条合并去重

### 根因
长文档被切分为多段/多文档并行审核，同一规则产生 N 条结论，此前直接逐条落库 → 20 规则输出远超 20 条，且前端/导出无「每规则一条」聚合视图。

### 改动
- `review_engine.py` 新增 `_aggregate_rule_results(findings, rules)`：按 `rule_id` 聚合为每规则一条，状态取 `fail > warn > unknown > pass` 最严重项，保留 `issue_count` 与样例。
- 通过 `rule_results` 事件落库（`task_store` 新增 `rule_results` 字段，`to_summary / to_detail` 暴露；`review.py:on_event` 处理该事件）。
- 配套：分段合并 `_merge_segment_raws`（累加语义）+ `_normalize_findings` 的 typo 指纹去重保证段内去重；此处补全「每规则仅一条」的最终聚合。

### 验证
25 规则 → `rule_results` 数 = 25、唯一 `rule_id` = 25（含合并批次的 3 条校对规则各一条）。
输出覆盖三处：前端详情页「规则审核结果（25）」表 + Word 导出「**三、规则审核结果汇总（25 行）**」+ PDF 导出同构章节。

---

## 4. 衍生修复 A：登录后任务列表永远为空（complaint① 更底层根因）

### 根因
`useReviewTasks()` 在 `App.tsx` 顶层挂载，其 `loadList()` 在**登录态建立前**触发 → 无令牌 → `/api/review/tasks` 401 → 列表 `[]`；`loadList` 是 `useCallback([])` 稳定引用，登录后无副作用再触发 → 列表永久为空。用户因此既看不到历史任务也看不到任何进度。

### 改动
- `frontend/src/hooks/useReviewTasks.ts`：删除挂载即触发的 `useEffect(() => { void loadList() }, [loadList])`。
- `frontend/src/App.tsx`：在 `const tasks = useReviewTasks()` 后加
  `useEffect(() => { if (authUser) void tasks.loadList() }, [authUser, tasks.loadList])`。

### 验证
真实浏览器：登录后「历史任务 **28**」、表格 11 行可见；点开任务渲染「规则审核结果（25）」。

---

## 5. 衍生修复 B：PDF 导出整条 500

### 根因
`POST /api/export/report?format=pdf` 报「PDF 导出需要安装 docx2pdf 库」；容器无 LibreOffice / docx2pdf / reportlab（仅 PyMuPDF，不能从零排版）→ PDF 导出从未可用。

### 改动
- `backend/requirements.txt` 加 `reportlab==4.2.5`。
- `backend/services/report_exporter.py`：`export_report` 的 pdf 分支改调新增 `ReportExporter.export_pdf()`（`SimpleDocTemplate` + `UnicodeCIDFont('STSong-Light')` 内置中文字体，原生渲染五章节：摘要 / 结论详情 / 规则审核结果汇总 / 一致性 / 法规检索）；移除 docx2pdf 依赖。

### 验证
任务 `b41cd2a0de40`：`/api/export/report?format=pdf` → **200、55KB、%PDF 头有效**；抽文含「规则审核结果汇总」+ 25 规则名 + 样例 + 一致性章节。

---

## 6. 部署变更文件清单

**后端**
- `backend/services/review_engine.py`（校对规则合并单批、`as_completed` 单调进度、`_aggregate_rule_results`、`_proofread_by_segments`、`_merge_segment_raws`、`_normalize_findings` typo 去重）
- `backend/routers/review.py`（`_build_on_event` 进度护栏、`rule_results` 事件）
- `backend/services/task_store.py`（`rule_results` 字段、`reap_orphans` 陈旧回收）
- `backend/services/report_exporter.py`（规则汇总章节、`export_pdf`、reportlab）
- `backend/routers/export.py`（`ExportRequest.rule_results`）
- `backend/requirements.txt`（`reportlab==4.2.5`）

**前端**
- `frontend/src/hooks/useReviewTasks.ts`（删除挂载无令牌自动加载）
- `frontend/src/App.tsx`（`authUser` 就绪后加载列表）
- `frontend/src/types/index.ts`（`RuleResult` 接口 + `rule_results` 字段）
- `frontend/src/pages/TaskDetailPage.tsx`（`ruleResults` prop）
- `frontend/src/components/ResultPanel.tsx`（规则结果表 + 导出传 `rule_results`）
- `frontend/src/services/api.ts`（`exportReport` 加 `rule_results`）

**部署注意**：改前端需本地 `npm run build` → `docker compose build frontend && up -d frontend`；改后端需 `docker compose build backend && up -d backend`（`/app/backend` 是构建时 COPY，**非 bind 挂载**，必须重建镜像）。

---

## 7. 验证证据汇总

| 项 | 证据 | 结论 |
|---|---|---|
| 进度 0% | 真实任务 `8261c6fddbec` 80→100 单调 | ✅ PASS |
| 列表空 | 真实浏览器「历史任务 28」行可见 | ✅ PASS |
| >15min 瓶颈 | 25 规则任务 484s（≈8min） | ✅ PASS |
| 20→20 结果 | `rule_results` 25 / 唯一 25 | ✅ PASS |
| Word 导出 | 57KB，规则汇总 25 行 | ✅ PASS |
| PDF 导出 | 55KB，含五章节 | ✅ PASS |
| 服务健康 | `:9090` / `:9100` 均 200 | ✅ PASS |

---

## 8. 残留 / 后续

- ⚠️ 默认 `admin/admin123` 仍可用，上线前应改密（安全遗留）。
- 历史验证产物：`审核报告_b41cd2a0de40_规则汇总验证.docx`、`报告_pdf_验证.pdf`。
- 临时验证脚本（`backend/verify_per_rule.py`、`diag_live/`）已清理。
