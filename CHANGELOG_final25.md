# final25 变更清单（招投标审核反幻觉 + 错别字通用性修复）

## 一、本次新增/修复的防护（引擎层，后端 review_engine.py）

| 防护函数 | 触发条件 | 处置 |
| --- | --- | --- |
| `_unresolved_file_reference` | fail/warn 且 involved_files 全部不在本次送审文件内 | 降级 unknown（file_unresolved），清空定位字段 |
| `_duplicate_claim_unsubstantiated` | 断言"重复/雷同"但引文比对不出重复主体 | 降级 unknown（duplicate_unverified） |
| evidence_missing（既有增强） | fail/warn 且 evidence 为占位词/无依据 | 降级 unknown（evidence_missing） |
| `_typo_claim_is_entity_equivalence` | 错别字结论声明错别字 + 出现≥2个不同主体名 + 用"应为/而非/改为" | 纠正为 pass（typo_entity_equivalence），专有名词非错别字 |
| `_explicitly_reports_no_violation` 否定前缀修复（既有） | 修复"不符合要求"被子串误判为"符合要求" | 纠正为 pass |

## 二、规则文案重写（18 条 dec-*，四要素：判定对象/范围/认定标准/不触发条件）

> 仅替换 description/checkpoints，保留库内 severity、doc_types、structured 配置（外科手术式迁移 migrate_rule_texts.py）。

### dec-01 评标报告必备要素完整性

- **修改前说明**：仅基于提供的评审报告等材料，核查报告是否载明法定必备内容要素，确保评审过程在文档层面可追溯、结论可复核（不涉及线下评标委员会组成合法性等现场核验）。
- **修改后说明**：判定对象：本次提供的评审报告全文（含其引用的附件）。判定范围：仅核查法定必备要素是否载明，不审要素内容的实体合法性与线下评标委员会组成。认定标准：要素在全文中以任何等价表述出现即视为已载明（不要求逐字匹配模板）；某要素全文及附件均无对应内容时才计「缺失」。不触发条件：全部要素齐备→pass；仅部分要素缺失→fail 并逐项列出缺失要素名；材料未提供或要素无法辨识（图片/扫描不清）→unknown，不得臆测。
- **审核要点变更**：
  - 要素1 评委名单及签字页：报告是否载明评标委员会成员名单并有成员签字（或载明签字页）
  - 要素2 开标记录与评标标准/方法说明是否包含
  - 要素3 是否记录各投标人的评分或评审意见及排序
  - 要素4 废标/否决投标是否逐条说明具体理由与依据
  - 要素5 是否载明中标候选人推荐意见及推荐人数
  - 要素6 是否有评标委员会全体成员签字确认页

### dec-02 评审报告否决投标理由载明与具体性

- **修改前说明**：核查提供的评审报告中否决（废标）投标是否逐条载明具体理由与依据、理由是否可复核（仅审文档内容，不评判线下否决决定的实体合法性）。
- **修改后说明**：判定对象：评审报告中否决（废标）投标相关段落。判定范围：仅审否决理由是否逐条载明、具体、可复核；不评判否决决定本身的实体合法性与线下程序。认定标准：每个被否决投标人在报告中须有对应否决条目，条目须引用招标文件具体条款编号或指明具体事实；仅有「不符合要求」「答辩不理想」等无条款/事实指向的笼统表述视为不具体。不触发条件：报告未发生否决情形→直接 pass；否决理由已引用具体条款/事实→pass；报告未提供或无否决记载可核对→unknown。
- **审核要点变更**：
  - 每个被否决投标人是否有对应的否决条目（逐条对应，不得笼统一句带过）
  - 否决条目是否引用招标文件具体条款编号或指明具体事实
  - 否决理由是否可复核：依据条目能在招标文件/投标文件中找到对应内容
  - 是否存在应否决未否决或不应否决而否决的明显矛盾（须列出矛盾双方原文）

### dec-03 评审打分异常与一致性

- **修改前说明**：基于提供的各评委打分结构化数据，识别打分畸高畸低、趋同或协商评分等异常（纯数据分析，不涉及评委线下行为）。
- **修改后说明**：判定对象：提供的各评委打分数据（结构化数据或打分表）。判定范围：仅对打分数值做离群/趋同分析；不审评委线下行为。认定标准：判「畸高畸低」必须写出具体数值证据（该评委评分、其他评委评分、差值）；判「趋同」必须列出相互趋同的得分序列；无数值支撑的怀疑一律不得计入。不触发条件：打分数据未提供→unknown；分数差异可由评标标准解释且无数值异常→pass；严禁凭空计算或虚构分值。
- **审核要点变更**：
  - 各评委对同一投标人打分是否存在明显离群（须列出该评委分值与其他评委分值的数值对比）
  - 是否存在不同投标人得分高度趋同、缺乏区分度（须列出趋同的得分序列）
  - 主观分评分档次是否与评分标准对应（偏离时列出标准原文与实际分值）
  - 是否存在替补评委未独立评分的情形（以报告载明信息为准）

### dec-04 评审报告候选人排序与评标办法一致性

- **修改前说明**：比对提供的评审报告，核查中标候选人排序是否与报告载明的评标办法一致（仅审文档内部一致性）。
- **修改后说明**：判定对象：评审报告载明的中标候选人排序及报告载明的评标办法。判定范围：仅审报告内部「办法与排序」的逻辑一致性。认定标准：按报告载明的排序规则（综合得分高→低，或经评审最低价优先）逐名比对实际排序；得分相等的并列按报告说明处理。不触发条件：报告未载明排序或办法→unknown；排序与办法一致→pass；其他排序依据类问题不在本规则范畴。
- **审核要点变更**：
  - 候选人排序是否与报告载明评标办法的排序规则一致（逐名比对得分）
  - 推荐中标候选人人数是否与评标办法约定一致
  - 第一中标候选人是否为排序首位
  - 排序依据的表述是否与评标办法表述一致

### dec-05 定标结果与评审报告推荐候选人一致性

- **修改前说明**：比对提供的定标结果结构化数据与评审报告，核查确定的中标人是否来自报告推荐的中标候选人（仅审提供数据的一致性，不审线下定标决策合法性）。
- **修改后说明**：判定对象：定标结果载明的中标人名称 vs 评审报告推荐的中标候选人名单。判定范围：仅做这两个名单之间的归属比对。认定标准：名称按逐字比对（忽略首尾空格与全半角差异；不忽略简称/别称/分公司——名称不完全一致即计异常，除非材料载明系同一主体的更名/曾用名）；中标人不在推荐名单内→fail 并同时摘录中标人名称与推荐名单原文。不触发条件：中标人恰为推荐候选人之一→pass；定标结果或评审报告未提供→unknown；本规则不做投标人名单内部查重、不做算术演算。
- **审核要点变更**：
  - 确定的中标人是否逐字出现在评审报告推荐的中标候选人名单中
  - 是否存在推荐候选人之外的主体被定为中标人（摘录双方原文比对）
  - 定标理由是否引用评审报告结论（载明与否，不作实体评判）

### dec-06 定标结果与招标文件授标办法一致性

- **修改前说明**：比对提供的定标结果与招标文件，核查授标方式是否与招标文件载明的一致（仅审提供材料一致性）。
- **修改后说明**：判定对象：定标结果载明的授标方式 vs 招标文件载明的中标人确定方式。判定范围：仅比对两处载明的方式是否一致。认定标准：定标方式（综合得分第一/最低评标价/谈判定标等）须能对应到招标文件的约定；材料体现的谈判、二次报价等改变结果的情形须摘录原文。不触发条件：招标文件未载明确定方式→unknown；方式一致→pass；授标金额、排序比对不在本规则范畴（分别属 dec-10/dec-05）。
- **审核要点变更**：
  - 定标方式是否与招标文件约定的中标人确定方式一致
  - 是否存在以谈判、二次报价等方式实质改变中标结果（以材料载明为准，须摘录原文）
  - 是否对中标候选人提出招标文件未规定的额外条件（须摘录该条件原文）

### dec-07 定标与评标文档时间逻辑一致性

- **修改前说明**：比对提供的定标审批材料与评标报告签署日期，核查时间逻辑是否合理（仅审文档日期，不审线下流程时效）。
- **修改后说明**：判定对象：定标审批材料与评标报告中出现的日期字段（定标审批日期、评标报告签署日期、中标候选人公示日期）。判定范围：仅比对上述日期字段的先后逻辑；不审线下流程时效，不审投标人名称、评分、金额等任何非日期事项（那些属其他规则范畴，即使发现异常也不得在本规则下输出）。认定标准：定标审批日期不得早于评标报告签署日期；定标日期不得早于公示起始日期（仅当材料载明公示日期时才比对）；判 fail 必须逐条列出「字段名+日期值」的倒置对（如：定标审批 2024-01-01 早于评标报告签署 2024-03-01）。不触发条件：日期逻辑正常→pass；相关日期字段缺失或未提供材料→unknown，不得凭空白臆测补签/倒置。
- **审核要点变更**：
  - 定标审批日期与评标报告签署日期比对：审批日期早于签署日期即倒置（列出两个日期值）
  - 定标日期与中标候选人公示起始日期比对：定标早于公示即倒置（材料载明公示日期时才比对）
  - 是否存在补签痕迹：仅以材料中可见的日期涂改、明显晚签记载为据，不得推测

### dec-08 中标人资格条件材料有效性

- **修改前说明**：基于提供的中标人资格证明材料，核查其营业执照、资质等是否仍在有效期内、仍满足招标文件门槛（仅审提供材料，不审线下实质履约能力）。
- **修改后说明**：判定对象：提供的中标人资格证明材料（营业执照、资质证书等）载明的有效期与资质事项。判定范围：仅审材料载明的有效期与门槛符合性。认定标准：证书有效期截止日期不早于定标日期视为有效；资质等级/范围与招标文件列明门槛逐项比对。不触发条件：材料未提供→unknown；均在有效期内且满足门槛→pass；履约能力等线下实质判断不在本规则范畴。
- **审核要点变更**：
  - 中标人营业执照、资质证书有效期截止是否不早于定标日期（列出证书名与有效期）
  - 资质等级/范围是否仍满足招标文件列明的资格门槛（逐项比对）
  - 材料中是否载明影响履约的重大变化（以材料明确记载为准）

### dec-09 三方文档投标人名称一致

- **修改前说明**：比对评审报告、定标结果、中标通知书（如有）中的中标人名称，核查是否一致。
- **修改后说明**：判定对象：评审报告、定标结果、中标通知书（如有）中的中标人/投标人名称与统一社会信用代码。判定范围：仅做跨文档的名称/代码一致性比对；投标人名单内部是否重复不属本规则（也无此要求）。认定标准：名称逐字比对，仅忽略首尾空格与全半角差异；简称、别称、分公司与总公司视为不一致，除非材料明确载明系同一主体；判 fail 必须并排摘录不一致的两处原文（文档A写X、文档B写Y）。不触发条件：名称（及代码）完全一致→pass；相关材料缺失→unknown；严禁在无任何两处不一致原文的情况下凭空断言「不一致/重复」。
- **审核要点变更**：
  - 评审报告推荐的中标候选人与定标结果中标人名称是否逐字一致
  - 定标中标人与中标通知书中标人名称是否逐字一致
  - 统一社会信用代码在各文档是否一致
  - 名称存在简称/全称差异时是否被材料明确载明为同一主体（未载明即计不一致）

### dec-10 中标金额一致性

- **修改前说明**：核查中标价在评审报告、定标结果、中标通知书中是否完全一致。
- **修改后说明**：判定对象：评审报告、定标结果、中标通知书中载明的中标价。判定范围：仅做跨文档金额数值比对。认定标准：先统一口径（大写/小写换算、含税/不含税按材料载明口径），换算后数值完全相等才计一致；不一致必须列出两边数值与出处；换算必须写出算式（如 大写壹佰万元=1,000,000元）。不触发条件：各文档金额一致→pass；金额字段缺失→unknown；严禁凭空计算或虚构差值。
- **审核要点变更**：
  - 评审报告中标价与定标结果中标价是否一致（统一口径后比对数值）
  - 中标通知书金额与定标结果金额是否一致
  - 大小写金额换算是否相符（写出算式），含税口径是否一致

### dec-11 工期与交付期一致性

- **修改前说明**：核查工期或交付期在各文档是否一致。
- **修改后说明**：判定对象：各文档载明的工期/交付期。判定范围：仅做跨文档工期数值或日期比对。认定标准：以日历天/具体日期统一后比对；「90日历天」与「三个月」须换算并写出依据。不触发条件：一致→pass；字段缺失→unknown；不得把表述差异（如「约90天」vs「90日历天」）直接判不一致，须评估实质含义。
- **审核要点变更**：
  - 评审报告承诺工期与定标结果载明工期是否一致（换算后比对）
  - 中标通知书工期与投标文件承诺是否一致

### dec-12 与招标文件实质性要求一致

- **修改前说明**：核查定标结果是否偏离招标文件实质性要求（基于提供材料）。
- **修改后说明**：判定对象：定标结果/中标人响应情况 vs 招标文件实质性条款。判定范围：仅比对材料中可核对实质性条款（资质、工期、质保、付款等）的响应。认定标准：判 fail 必须摘录招标文件条款原文与中标人响应原文并排比对。不触发条件：无偏离→pass；招标文件条款或响应材料缺失→unknown；评分高低类差异不属本规则。
- **审核要点变更**：
  - 中标人是否仍满足招标文件实质性条款（逐条摘录比对）
  - 定标阶段是否新增招标文件未载明的义务（须摘录该义务原文）

### dec-13 评审报告附件完整性

- **修改前说明**：核查正文引用的评审报告及支撑材料是否齐全（基于提供附件）。
- **修改后说明**：判定对象：正文提及的评审报告及支撑材料附件。判定范围：仅核对「正文提及↔附件实际提供」的对应关系。认定标准：正文引用的每份附件须能找到实物（编号/名称对应）；判 fail 列出缺失附件的编号/名称。不触发条件：全部附件齐备→pass；正文未提及任何附件→pass；无法确认是否随附→unknown。
- **审核要点变更**：
  - 正文提及的评审报告、打分表是否全部提供（逐项对应）
  - 附件编号与正文引用是否对应
  - 是否存在应附未附的评审底稿（以正文提及为准）

### dec-14 评审报告签字盖章规范

- **修改前说明**：核查提供的评审报告签字盖章是否齐全、形式规范（仅审文档形式，不审线下签字真实性）。
- **修改后说明**：判定对象：评审报告文本中签字、盖章的载明与形式。判定范围：仅审文档形式是否齐全规范；不审签字/印章真实性。认定标准：以材料可见信息为准（签字页、盖章描述、骑缝章痕迹）；OCR/预览无法辨识时填 unknown 而非判缺失。不触发条件：签字盖章齐全且清晰→pass；材料为无签章电子文本且无法核验→unknown。
- **审核要点变更**：
  - 评审报告是否逐页盖章或加盖骑缝章（以材料可见为准）
  - 签字盖章是否清晰、无遗漏（成员逐一核对，列缺席者）
  - 公章名称与招标人、代理机构名称是否一致

### dec-15 评审报告格式规范

- **修改前说明**：评审报告格式应规范统一。
- **修改后说明**：判定对象：评审报告的版式结构。判定范围：仅审章节编号、目录页码、表格结构等格式项。认定标准：判 fail 须指出具体位置（章节/页码）与问题。不触发条件：格式无实质问题→pass；纯观感建议（不影响使用）不得判 fail；文字错误属 dec-17、数据错误属 dec-16，均不在本规则输出。
- **审核要点变更**：
  - 章节编号、目录、页码是否完整对应（指出具体缺失位置）
  - 表格是否有表头、表名，结构完整
  - 格式是否符合单位模板要求（以材料可比对信息为准）

### dec-16 评审报告数据准确性

- **修改前说明**：报告内数据、金额、日期应准确一致。
- **修改后说明**：判定对象：报告内数据、金额、日期、评分汇总。判定范围：仅审报告内部数据的准确与一致。认定标准：所有算术结论必须写出算式（明细求和=汇总、得分×权重=加权分）；判 fail 列出错处两边的数值。不触发条件：数据一致且计算正确→pass；数据缺失→unknown；严禁凭空计算或虚构差值。
- **审核要点变更**：
  - 金额单位、大小写是否统一（换算写出算式）
  - 日期格式是否统一且逻辑合理
  - 评分汇总与明细是否一致（求和验算并列出算式），有无计算错误

### dec-17 错别字与表述规范

- **修改前说明**：评审报告文字应准确、无错别字。
- **修改后说明**：判定对象：评审报告文字表述。判定范围：仅审错别字、术语规范、语句通顺。认定标准：每处错别字须给出「错字→正字」及所在原句；不得将标点/格式问题计入错别字。不触发条件：无错别字→pass；格式问题属 dec-15。
- **审核要点变更**：
  - 是否存在形近字、音近字错别字（如「保证金」误为「保障金」，逐处列出原句）
  - 专业术语使用是否规范统一
  - 语句是否通顺、无歧义

### dec-18 附件引用一致性

- **修改前说明**：正文与附件内容应一致。
- **修改后说明**：判定对象：正文与附件内容的数据对应关系。判定范围：仅审正文引用数据与附件表格数据是否一致。认定标准：判 fail 须并排摘录正文数值与附件数值。不触发条件：一致→pass；附件缺失→unknown（附件完整性属 dec-13）；不得臆测未提供附件的内容。
- **审核要点变更**：
  - 正文数据是否与附件表格一致（并排摘录比对）
  - 附件内容是否与正文描述匹配
  - 是否存在附件缺失或错放（以正文引用为线索）

## 三、提示词纪律（review_rule v5→v6）

- 新增「要素来源与抽取纪律」：任何结论须关联真实送审文件；一致性规则只比真实抽取到的要素，单边缺失填 unknown；工期等时间字段逐字摘录、未写明即跳过；fail/warn 须显式标注来源文件；未抽取要素标注【跳过】。
- 新增「错别字检测的通用性纪律（硬约束）」：错别字只判行文错别字，不携带业务属性/规则；专有名词（公司/机构名）不得当错别字，把两个主体名当正误关系判 fail 会被系统纠正为 pass。

## 四、建议回归验证用例

### 反幻觉（test_source_guard / test_duplicate_guard / test_evidence_guard）
- 正例(应触发降级)：fail 且 involved_files=虚构文件 → unknown；断言"投标人名称均重复"但引文无重复主体 → unknown；fail 且 evidence="无" → unknown。
- 反例(应保持)：fail 且 involved_files=真实文件 → fail；真实行文错别字"保障金→保证金" → fail 保留 typo。
### 错别字通用性（test_typo_entity_guard）
- 正例(应纠正 pass)：错别字结论称"南京大六文化传媒有限公司"应为"南京尊致文化发展有限公司"（两主体名互指）→ pass。
- 反例(应保持 fail)：真实行文错别字"保障金→保证金" → fail；同一主体名内笔误"有现公司→有限公司"且模型称"笔误" → fail（真实笔误保留）。
