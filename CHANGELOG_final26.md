# 变更记录 final26 —— 审核推理清晰度与定位/引用精度优化

> 目标：让模型生成的审核推理「逻辑清晰、简明易懂」，且原文定位精确、引用忠实不失真。
> 改动分两层：**提示词纪律层**（最大杠杆）+ **规则审核点表述层**（逐条原子化）。

## 一、提示词 `_BUILTIN_REVIEW_RULE`（v6 → v7，prompt_store.py）

### 1.1 强化 JSON 字段定义（detail / evidence / location）
- **detail**：改为强制按 `【定位→取证→校验→结论】` 四步书写，并点明每步要求（定位到章节/段落/行号/字段名、取证逐字摘录、校验写完整算式、结论与 status 一致）。
- **evidence**：明确「须与原始材料逐字一致，不得改写/概括/意译」；允许首尾「……」省略无关部分（中间不得跳跃省略、不改语义）；须注明文件与精确位置。
- **location**：从「第X页/第X章」升级为「章节/段落/行号/字段名」（例：《XX投标函》'投标总价'字段 / 第3章第2.1条 / 第5页第3段 / 打分表第2行）；继续禁泛称。

### 1.2 新增三条硬纪律块（置于输出要求末尾）
1. **【推理过程书写规范（分步·因果明确）】**
   - 四步顺序：①定位 ②取证 ③校验（数值/日期写完整算式与结果）④结论（从上文推出，不得突兀翻转）。
   - 严禁先下结论再倒找理由（避免 status 与 detail 矛盾被降级）；严禁"经评审/经核查"等无推理模板句。
   - 多要素规则逐要素给四步，序号/空行分隔，避免混为一谈。
2. **【原文定位规范（精确·可回溯）】**
   - location 精确到"单文件+具体位置"五类（章节/条款/段落/行号/字段名）。
   - 跨文件比对时 evidence 内并排摘录两边位置，location 填主文件。
   - 无法定位（图片/OCR 不清）不得编造，改注"位置不可辨识"并据情形填 unknown。
3. **【原文引用规范（忠实·不失真）】**
   - 只摘录、不改写、不概括、不意译；数字/单位/标点/称谓原样保留。
   - 首尾「……」省略不得跳跃、不得改语义；不得为"通顺"改写措辞。
   - 严禁把原文"翻译"成自己的话（如"前附表3.2条"概括为"相关规定"）。
   - 证据须能支撑结论，证据与结论不匹配→判无依据降级。

## 二、规则审核点表述优化（gen_decision_rules.py，18 条 dec-*）

规则说明（四要素：判定对象/范围/认定标准/不触发条件）已较严谨，**保持不变**；
仅将每条 `checkpoints` 改写为统一模板：**「核对动作 + 明确判定标志」**，使模型分步核验、因果清晰、定位精确。

改写要点（全部 18 条）：
- 每条审核点先写"核对什么"（定位到具体位置/字段），再写"出现什么计为异常/缺失、满足什么不计"。
- 示例对比：
  - 旧 dec-07：「定标审批日期与评标报告签署日期比对：审批日期早于签署日期即倒置」
  - 新 dec-07：「审批vs签署：定标审批日期是否早于评标报告签署日期（列出两字段名与日期值）；早于即倒置计异常」
- 一致性类（dec-09/10/11/12）每条均要求"并排摘录两处原文与各自精确位置"，强化可溯源性。
- dec-17 专名条款明确"误判即纠正 pass"，与 final25 错别字通用性纪律闭环。
- 数值类（dec-03/10/16）保留"须列出数值对比/写出算式"的硬要求。

## 三、生效方式（部署侧）

1. **提示词**：需 `POST /api/prompts/review_rule/reset`（重置为内置默认 v7）方可生效；若已在管理页自定义，请以内置新文案为准手动保存版本。
2. **规则**：在后端容器内执行 `python /app/migrate_rule_texts.py`（外科手术式更新 description/checkpoints，保留 severity/doc_types/structured/enabled）。
3. 前端无感知（规则文案由后端推送）；历史任务快照不回改，重跑即按新逻辑。

## 四、回归关注点（建议复验）
- 推理过程是否出现"定位→取证→校验→结论"四步结构（detail 字段）。
- fail/warn 的 evidence 是否为逐字摘录、location 是否精确到字段/章节/段落/行号。
- 一致性类结论是否并排列出两处原文与位置。
- 错别字规则是否仍只对普通行文错字判 fail，专有名词不误判。
